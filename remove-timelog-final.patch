--- a/worker/src/index.ts
+++ b/worker/src/index.ts
@@ -11,7 +11,7 @@
 import {
   sqliteTable, text, integer, real, primaryKey,
 } from 'drizzle-orm/sqlite-core'
-import { eq, and, desc, asc, gte, lte, sql, isNull, not, or } from 'drizzle-orm'
+import { eq, and, desc, asc, gte, lte, sql, not, or } from 'drizzle-orm'
 
 // ============================================================
 // SECTION 2: ENV TYPE
@@ -186,16 +186,6 @@
   created_at:   text('created_at').notNull().default(sql`(datetime('now'))`),
 })
 
-const timeLogs = sqliteTable('time_logs', {
-  id:          text('id').primaryKey(),
-  user_id:     text('user_id').notNull().references(() => users.id),
-  clock_in:    text('clock_in').notNull(),
-  clock_out:   text('clock_out'),
-  edited_by:   text('edited_by').references(() => users.id),
-  edit_reason: text('edit_reason'),
-  created_at:  text('created_at').notNull().default(sql`(datetime('now'))`),
-})
-
 const systemSettings = sqliteTable('system_settings', {
   key:        text('key').primaryKey(),
   value:      text('value').notNull(),
@@ -1092,77 +1082,6 @@
 
 
 // ============================================================
-// SECTION 12: TIME LOG ROUTES
-// ============================================================
-
-app.get('/api/time-logs', async (c) => {
-  const actor = c.get('user')
-  const db = c.get('db')
-  const { user_id, date_from, date_to } = c.req.query()
-  // Crew can only see their own; admin sees all
-  const targetId = actor.role === 'admin' ? (user_id ?? null) : actor.id
-
-  let query = db.select({
-    id: timeLogs.id, user_id: timeLogs.user_id, clock_in: timeLogs.clock_in,
-    clock_out: timeLogs.clock_out, edited_by: timeLogs.edited_by,
-    edit_reason: timeLogs.edit_reason, created_at: timeLogs.created_at,
-    user_name: users.name,
-  }).from(timeLogs)
-    .innerJoin(users, eq(users.id, timeLogs.user_id))
-    .orderBy(desc(timeLogs.clock_in)).$dynamic()
-
-  if (targetId) query = query.where(eq(timeLogs.user_id, targetId))
-  if (date_from) query = query.where(gte(timeLogs.clock_in, date_from))
-  if (date_to) query = query.where(lte(timeLogs.clock_in, date_to + 'T23:59:59'))
-
-  const list = await query
-  return jsonOk(list)
-})
-
-app.post('/api/time-logs/clock-in', async (c) => {
-  const actor = c.get('user')
-  const db = c.get('db')
-  // Check not already clocked in
-  const open = await db.select({ id: timeLogs.id }).from(timeLogs)
-    .where(and(eq(timeLogs.user_id, actor.id), isNull(timeLogs.clock_out))).get()
-  if (open) return jsonErr('Already clocked in')
-  const id = uid()
-  const clockIn = nowISO()
-  await db.insert(timeLogs).values({ id, user_id: actor.id, clock_in: clockIn, created_at: clockIn })
-  return jsonOk({ id, clock_in: clockIn })
-})
-
-app.post('/api/time-logs/clock-out', async (c) => {
-  const actor = c.get('user')
-  const db = c.get('db')
-  const open = await db.select({ id: timeLogs.id, clock_in: timeLogs.clock_in }).from(timeLogs)
-    .where(and(eq(timeLogs.user_id, actor.id), isNull(timeLogs.clock_out)))
-    .orderBy(desc(timeLogs.clock_in)).get()
-  if (!open) return jsonErr('Not clocked in')
-  const clock_out = nowISO()
-  await db.update(timeLogs).set({ clock_out }).where(eq(timeLogs.id, open.id))
-  return jsonOk({ id: open.id, clock_in: open.clock_in, clock_out })
-})
-
-app.put('/api/time-logs/:id', async (c) => {
-  const actor = c.get('user')
-  if (actor.role !== 'admin') return jsonErr('Admin only', 403)
-  const db = c.get('db')
-  const id = c.req.param('id')
-  const body = await c.req.json<{ clock_in?: string; clock_out?: string; reason: string }>()
-  if (!body.reason) return jsonErr('Reason required')
-  const old = await db.select().from(timeLogs).where(eq(timeLogs.id, id)).get()
-  if (!old) return jsonErr('Log not found', 404)
-  await db.update(timeLogs).set({
-    clock_in: body.clock_in ?? old.clock_in,
-    clock_out: body.clock_out ?? old.clock_out,
-    edited_by: actor.id, edit_reason: body.reason,
-  }).where(eq(timeLogs.id, id))
-  await createAuditLog(db, actor.id, 'edit_time_log', 'time_log', id, old, body, body.reason)
-  return jsonOk({ ok: true })
-})
-
-// ============================================================
 // SECTION 13: REPORT ROUTES
 // ============================================================
 
@@ -1300,45 +1219,6 @@
   })
 })
 
-app.get('/api/reports/work-hours', async (c) => {
-  const actor = c.get('user')
-  if (actor.role !== 'admin') return jsonErr('Admin only', 403)
-  const db = c.get('db')
-  const { date_from, date_to } = c.req.query()
-
-  let query = db.select({
-    id: timeLogs.id, user_id: timeLogs.user_id,
-    clock_in: timeLogs.clock_in, clock_out: timeLogs.clock_out,
-    user_name: users.name,
-  }).from(timeLogs)
-    .innerJoin(users, eq(users.id, timeLogs.user_id))
-    .$dynamic()
-
-  if (date_from) query = query.where(gte(timeLogs.clock_in, date_from))
-  if (date_to) query = query.where(lte(timeLogs.clock_in, date_to + 'T23:59:59'))
-
-  const logs = await query
-  const settings = await db.select().from(systemSettings).where(eq(systemSettings.key, 'hourly_rate')).get()
-  const hourlyRate = parseFloat(settings?.value ?? '80')
-
-  // Group by user and sum hours
-  const byUser: Record<string, { user_id: string; user_name: string; total_minutes: number }> = {}
-  for (const log of logs) {
-    if (!log.clock_out) continue
-    const mins = Math.round((new Date(log.clock_out).getTime() - new Date(log.clock_in).getTime()) / 60_000)
-    if (!byUser[log.user_id]) byUser[log.user_id] = { user_id: log.user_id, user_name: log.user_name, total_minutes: 0 }
-    byUser[log.user_id].total_minutes += mins
-  }
-
-  const summary = Object.values(byUser).map(u => ({
-    ...u,
-    total_hours: Math.round((u.total_minutes / 60) * 100) / 100,
-    estimated_salary: Math.round((u.total_minutes / 60) * hourlyRate * 100) / 100,
-  }))
-
-  return jsonOk({ summary, logs, hourly_rate: hourlyRate })
-})
-
 // ============================================================
 // SECTION 14: SETTINGS ROUTES
 // ============================================================
--- a/src/types.ts
+++ b/src/types.ts
@@ -188,7 +188,6 @@
   receipt_footer: string;
   sc_discount_pct: string;
   pwd_discount_pct: string;
-  hourly_rate: string;
   [key: string]: string;
 }
 
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -3275,7 +3275,7 @@
 
   const actionColor = useCallback((action: string) => {
     if (action.includes('delete') || action.includes('void') || action.includes('remove')) return 'red';
-    if (action.includes('create') || action.includes('open') || action.includes('clock_in')) return 'green';
+    if (action.includes('create') || action.includes('open')) return 'green';
     if (action.includes('update') || action.includes('edit') || action.includes('reset') || action.includes('close')) return 'yellow';
     return 'gray';
   }, []);
--- a/worker/migrations/0001_init.sql
+++ b/worker/migrations/0001_init.sql
@@ -161,7 +161,7 @@
 );
 
 -- ============================================================
--- AUDIT & TIME LOGS
+-- AUDIT LOGS
 -- ============================================================
 CREATE TABLE IF NOT EXISTS audit_logs (
   id TEXT PRIMARY KEY,
@@ -175,16 +175,6 @@
   created_at TEXT NOT NULL DEFAULT (datetime('now'))
 );
 
-CREATE TABLE IF NOT EXISTS time_logs (
-  id TEXT PRIMARY KEY,
-  user_id TEXT NOT NULL REFERENCES users(id),
-  clock_in TEXT NOT NULL,
-  clock_out TEXT,
-  edited_by TEXT REFERENCES users(id),
-  edit_reason TEXT,
-  created_at TEXT NOT NULL DEFAULT (datetime('now'))
-);
-
 -- ============================================================
 -- SETTINGS & INVENTORY
 -- ============================================================
@@ -225,7 +215,6 @@
 CREATE INDEX IF NOT EXISTS idx_sale_payments_sale   ON sale_payments(sale_id);
 CREATE INDEX IF NOT EXISTS idx_audit_entity         ON audit_logs(entity_type, entity_id);
 CREATE INDEX IF NOT EXISTS idx_audit_user           ON audit_logs(user_id);
-CREATE INDEX IF NOT EXISTS idx_time_logs_user       ON time_logs(user_id);
 CREATE INDEX IF NOT EXISTS idx_held_orders_expiry   ON held_orders(expires_at);
 CREATE INDEX IF NOT EXISTS idx_menu_items_category  ON menu_items(category_id);
 
@@ -239,5 +228,4 @@
   ('store_address',     '123 Mango St, Dasmariñas, Cavite'),
   ('store_contact',     '09XX-XXX-XXXX'),
   ('receipt_footer',    'Thank you for visiting MangoWarrior!'),
-  ('hourly_rate',       '80'),
   ('timezone',          'Asia/Manila');
\ No newline at end of file
