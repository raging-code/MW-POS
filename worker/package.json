{
  "name": "pos-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply pos-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply pos-db --remote",
    "db:create": "wrangler d1 create pos-db"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "drizzle-kit": "^0.20.14",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}