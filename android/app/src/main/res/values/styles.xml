<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        FIX: Changed android:background from @null to the app's warm-beige page
        background (#F0EDE8 = CSS var(--surface-page)).

        With @null the window background was transparent. On a hardware-accelerated
        surface this renders as black until the WebView's first paint, causing a
        visible black flash on cold start and on activity resume on slower devices
        (Adreno 308 / Mali-T830). Setting the window background to the same colour
        as the app body eliminates the flash entirely.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">#F0EDE8</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

</resources>